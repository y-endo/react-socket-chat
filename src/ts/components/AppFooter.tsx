import * as React from 'react';
import css from '~/scss/components/AppFooter.scss';

const AppFooter: React.FC = () => {
  return <footer className={css.footer}>フッター</footer>;
};

export default AppFooter;
