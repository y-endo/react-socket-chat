type Message {
  name: String!
  text: String!
  postedAt: String!
}

type Room {
  id: String!
  name: String!
  count: Int!
  messages: [Message]!
  createdAt: String!
}

type Query {
  rooms: [Room]
  room(id: String!): Room
}

type Mutation {
  addRoom(name: String!): Room
}
