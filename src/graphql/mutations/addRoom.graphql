mutation addRoom($id: String!, $name: String!, $count: Int!, $createdAt: String!) {
  addRoom(id: $id, name: $name, count: $count, createdAt: $createdAt) {
    id
    name
    count
    createdAt
  }
}
